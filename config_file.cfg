exp_id = ; # Experiment name/ID, e.g. "exp_8" 
fileroot = ; # Data location without image number, e.g. "./1St_8/1st_exp8_" 
extension = ; # Image format, e.g. ".BMP"
n_image = ; # Number of images to analyse 
incr_image = ; # Image increment 
time_conv = ; # Time conversion 
time_unit = ; # Time unit, e.g. "hours" 
err = ; # Horizontal error on the detection lines (int), e.g. 5 
n_tube = ; # Number of tubes in the experiment (int), e.g. 13
w_min = ; # Min width of the peaks (int), e.g. 1
w_max = ; # Max width of the peaks (int), e.g. 20
v_gap = ; # Mumber of pixels between the 3 detection lines, e.g. 2
h_position = ; # Horizontal position of the tubes, e.g. [18, 105, ..., 830, 897]
rotate_angle = ; # Angle to get the image horizontal 
top = ; # Start pixel top
bottom = ; # End pixel bottom 
left = ; # Start pixel left 
right = ; # End pixel right 
